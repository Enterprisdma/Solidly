# Sollidly 환경 변수 설정 파일
# 
# 사용 방법:
# 1. 이 파일을 .env로 복사
# 2. 아래 값들을 실제 API 키로 변경
# 3. .env 파일은 절대 Git에 커밋하지 않기 (.gitignore에 추가됨)

# OpenAI API 키 (필수는 아니지만 AI 기능 사용을 위해 권장)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API 키 (선택사항, Claude 사용 시)
# https://console.anthropic.com/ 에서 발급
ANTHROPIC_API_KEY=your_anthropic_api_key_here
